{
  "scripts": {
    "generate": "pnpm drizzle-kit generate:sqlite --schema=./schema.ts",
    "migrate": "node -r esbuild-register ./migrate.ts",
    "push": "drizzle-kit push:sqlite"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.515.0",
    "@aws-sdk/s3-request-presigner": "^3.515.0",
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.29.5",
    "pnpm": "^8.15.4",
    "sqlite": "^5.1.1"
  },
  "devDependencies": { 
    "@types/node": "^20.11.5",
    "drizzle-kit": "^0.20.14"
  }
}
